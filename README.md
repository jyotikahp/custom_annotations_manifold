# custom_annotations_manifold
To install this pip install git+https://github.com/jyotikahp/custom_annotations_manifold

AWS creds are needed to use this module

Store the credentials in ~/.aws/credentials
